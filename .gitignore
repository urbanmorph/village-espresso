node_modules
.DS_Store
.env
.env.*
!.env.example

# Build outputs
dist
build
.svelte-kit
.vercel
.playwright-mcp/

# Working notes — not for the public repo
supporting-docs/

# Large local-only artifacts
data/raw/
data/geo/*.pmtiles
data/geo/*.parquet
# intermediate filtered dumps + their cache signatures (re-buildable from parquet).
# Note: git has no trailing comments, so these patterns get their own lines.
data/geo/_*.json
data/geo/_*.key
data/geo/_*.csv
data/geo/_*.out.json
